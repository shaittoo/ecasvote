/**
 * Template id encoded in the ballot QR (with electionId + ballotToken).
 * Bump when the paper layout / QR schema changes.
 */
export const BALLOT_TEMPLATE_VERSION = "ballot-template-v1" as const;
